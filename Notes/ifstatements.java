public class HarmonicMean
{
	public static void main(String[] args) {
        int x = 3;
        int y = 4;


        boolean isAwesomeDay = true;
        boolean shouldWork = x < y; // expression - evaluated to one answer

        // != means not equal to

        String name = "Chelsea";

        // conditionals - conditions
        // pseudocode - writing out the structure
        // if (condition) {
        //     .. do stuff
        // }
        // condition ends up truw or false in the end
        // boolean is a variablet that holds a true or false statement

        // && combines expressions, means both must be true
        // || means either or
        // ^ either but not both



        if (isAwesomeDay && x > y) {
            // isAwesomeDay true
            
        }


        // antpatter - code works, but ther eis a better way - avoid

        if (isAwesomeDay == true) {
            // don't do this, instead to (isAwesomeDay)
        }



        // curly are sometimes optional
        if (condition) statement1; statement2;
        



        // compound proglems, like A, B, C, pick one
        if  (A) {
            // A is true
        } else if (B) {
            // A is not true
        } else {
            // fallback
        }

        // primitives and objects are  different when compared for equality
        if (x == y) {

        }
        if (name == "Alex" {
            // false, does not run
        })
        if ("Alex".equals(name)) {

        }

        




        
        
        
    }
}